{
    // расширяем основной конфиг всего проекта
    "extends": "../tsconfig.json",
    "compilerOptions": {
      "noEmit": true,
      // указываем что используются типы cypress
      "resolveJsonModule": true,
      "target": "es5",
      "lib": ["es5", "dom"],
      "types": ["cypress", "node"]
    },

    "include": ["../node_modules/cypress", "./**/*.ts"]
}
